import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kaviyarasan.kamatchisupermarket',
  appName: 'Kamatchi supermart',
  webDir: 'www',
  fullscreen: false
};

export default config;
